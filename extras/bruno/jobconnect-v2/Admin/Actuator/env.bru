meta {
  name: env
  type: http
  seq: 5
}

get {
  url: {{actuatorUrl}}/env
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.activeProfiles: isDefined
  res.body.activeProfiles: isArray
  res.body.activeProfiles: isNotEmpty
  res.body.propertySources: isDefined
  res.body.propertySources: isArray
  res.body.propertySources: isNotEmpty
}

settings {
  encodeUrl: true
  timeout: 0
}
