meta {
  name: info
  type: http
  seq: 2
}

get {
  url: {{actuatorUrl}}/info
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.os: isJson
  res.body.os: isNotEmpty
}

settings {
  encodeUrl: true
  timeout: 0
}
