meta {
  name: actuator
  type: http
  seq: 1
}

get {
  url: {{actuatorUrl}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body._links: isJson
  res.body._links: isNotEmpty
}

settings {
  encodeUrl: true
  timeout: 0
}
